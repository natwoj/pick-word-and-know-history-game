var buttonChange = document.getElementById("buttonChange");
buttonChange.addEventListener('click', changeThis);

function () {
var historyDiv = getElementById("history");
var adjective = "<span class='change'>" + getElementById("adjective").value + "</span>";




historyDiv.innerHTML = history;
}

